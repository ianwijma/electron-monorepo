import {PropsWithChildren} from 'react';

export type BaseWindowProps = PropsWithChildren<{}>

export const BaseWindow = ({children}: BaseWindowProps) => {

    return (
        <div>
            
        </div>
    )
}