export type FileSystemOptions = {
    allowRead: boolean,
    allowWrite: boolean,
}

export type FileSystemReturn = {}

export const fileSystem = ({allowRead, allowWrite}: FileSystemOptions): FileSystemReturn => {

    return {}
}